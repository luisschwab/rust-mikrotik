# mikrotik-graphviz

Graph construction and export for MikroTik device snapshots.

This crate builds topology graphs from `DeviceSnapshot` values and exports DOT,
SVG/PNG via Graphviz, and interactive HTML artifacts.

## Development

From the workspace root:

```text
cargo rbmt fmt
cargo rbmt lint
cargo rbmt test
cargo rbmt docs
```
